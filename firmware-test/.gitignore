# CubeMX-generated build outputs (kept locally, not in repo)
build/
Debug/
Release/
*.elf
*.bin
*.hex
*.map
*.list
*.o
*.d

# CubeIDE / CubeMX project metadata that doesn't need to be tracked
.metadata/
.settings/
.cproject
.project

# OS junk
.DS_Store
Thumbs.db
